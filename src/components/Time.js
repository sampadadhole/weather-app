import React from "react"

function Time(data){

    return(
        <div>
            {/* {data.sunrise && <h1>sunrise : {data.sunrise}</h1>}
            {data.sunset && <h1>sunset : {data.sunset}</h1>} */}
        </div>
    )
}
export default Time